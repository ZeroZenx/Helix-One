# Legacy backend entrypoint archive

`src/index.ts` represented an older architecture path and is no longer used.
Canonical runtime entrypoint is now:

- `src/server.ts`

This folder exists only to document that decision and avoid operator confusion.
